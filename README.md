## Project 1: BMI Calculator

**What it does:** 
Calculates Body Mass Index from user’s weight and height, and tells if person is Underweight, Normal, or Overweight.

**Formula used:**
BMI = weight / (height*- 2)
Weight in kg, Height in meters

**BMI Categories:**
- < 18.5  → Underweight
- 18.5 to 24.9 → Normal
- >= 25 → Overweight

`**Python Concepts Learned:**`
- `input()` → To take data from user
- `float()` → To convert text to decimal numbers
- `**` → For power, like height squared
- `if / elif / else` → For decision making
- `f-string` → For formatted output
- `:.2f` → To round BMI to 2 decimal places

**How to run:**
 ```bash  
python "bmi Calculator.py"
``` 

## Project 2: Temperature Converter

**What it does:**  
Converts temperature between Celsius and Fahrenheit.

**Python Concepts Learned:**
- `input()` and `float()` → For user input
- `if / elif / else` → For choice selection
- Formula math → `(celsius * 9/5) + 32`

**How to run:**
```bash
python "temperature converter.py"
```



